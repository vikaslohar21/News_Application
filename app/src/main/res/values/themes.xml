<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/deepBlue</item>
        <item name="colorPrimaryVariant">@color/deepBlue</item>
        <item name="android:statusBarColor">@color/deepBlue</item>
    </style>

    <style name="Theme.NewsApplication" parent="Base.Theme.NewsApplication" />
    
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/warmOrange</item>
    </style>
</resources>